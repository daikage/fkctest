
# IMPORTANT: Rotate your Gmail App Password now — it was posted publicly.
# Do NOT commit this file. Add ".env" to your .gitignore.


# Rotate your app password if you posted it publicly.
PORT=4000
NODE_ENV=production
SMTP_DEBUG=1

# Must match the account that the app password belongs to
GMAIL_USER=telkomanagedservice@gmail.com
GMAIL_APP_PASSWORD=ghke nnaa gkhw vxpa
TO_EMAIL=info@fortknoxchecks.com

# You can toggle these to test alternatives without touching code:
# 587 + STARTTLS (default)
SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# To try implicit TLS on 465, set:
SMTP_PORT=465
SMTP_SECURE=true
# Optional: force auth method if needed (try LOGIN). Leave unset for auto.
# SMTP_AUTH_METHOD=LOGIN